// Code generated by internal/codegen; DO NOT EDIT.

package collection

import (
	"fmt"
	"iter"
)

// ── Composable sub-interfaces ─────────────────────────────────────────
//
// Following Go's io.Reader / io.Writer / io.ReadWriter pattern, the
// primitive-collection API is built from small, single-concern
// interfaces that can be composed as needed.

// CharSized exposes the element count of a collection.
type CharSized interface {
	// Len returns the number of elements. Use x.Len() == 0 to test for
	// emptiness.
	Len() int
}

// CharIterable provides element-by-element traversal.
type CharIterable interface {
	// All returns an iter.Seq that yields all elements.
	All() iter.Seq[uint16]

	// ForEach calls the given function for each element.
	ForEach(f func(uint16))
}

// CharSearchable supports membership and predicate queries.
type CharSearchable interface {
	// Contains returns true if the collection contains the given value.
	Contains(value uint16) bool

	// AnySatisfy returns true if any element satisfies the predicate.
	AnySatisfy(predicate func(uint16) bool) bool

	// AllSatisfy returns true if all elements satisfy the predicate.
	AllSatisfy(predicate func(uint16) bool) bool

	// NoneSatisfy returns true if no element satisfies the predicate.
	NoneSatisfy(predicate func(uint16) bool) bool
}

// CharConvertible supports bulk conversion to a slice.
type CharConvertible interface {
	// ToSlice returns all elements as a slice.
	ToSlice() []uint16
}

// ── Composed collection interfaces ────────────────────────────────────

// CharCollection is the full read-only interface for any collection of
// uint16 values.  It composes the smaller sub-interfaces above, so a
// caller that only needs iteration can accept CharIterable while code
// that needs everything can accept CharCollection.
//
// Satisfied by: CharArrayList, CharHashSet, CharHashBag,
// CharArrayStack, CharTreeSet, and their immutable variants.
type CharCollection interface {
	CharSized
	CharIterable
	CharSearchable
	CharConvertible
	fmt.Stringer
}

// CharMutableCollection extends CharCollection with mutation operations.
// Satisfied by: CharArrayList, CharHashSet, CharHashBag, CharArrayStack.
type CharMutableCollection interface {
	CharCollection

	// Clear removes all elements.
	Clear()
}

// ── Category interfaces ────────────────────────────────────────────────
//
// These distinguish *what kind of collection* is required without naming
// a concrete type. They mirror Java's IntList / IntSet / IntBag / IntStack
// hierarchy and the matching trait/comptime layers in Rust and Zig.
//
// Note: every mutable non-stack category exposes the unified Add(value) bool
// (the CharAdder capability): lists and bags always return true (they always
// accept the element), sets return whether the value was newly inserted. Stacks
// use Push instead. The bool is collection-specific insertion info that bulk
// loaders (seq.Into) ignore.

// CharList is the read-only interface for ordered lists with positional
// access. Satisfied by: CharArrayList, ImmutableCharArrayList.
type CharList interface {
	CharCollection

	// Get returns the element at the given index. It panics on out-of-range index.
	Get(index int) uint16

	// IndexOf returns the index of the first occurrence of value, or -1 if absent.
	IndexOf(value uint16) int
}

// CharMutableList extends CharList + CharMutableCollection.
// Satisfied by: CharArrayList.
type CharMutableList interface {
	CharList
	CharMutableCollection

	// Add appends a value to the end of the list. Returns true always (a list
	// always accepts the element); the bool is the CharAdder contract.
	Add(value uint16) bool

	// Set sets the value at the given index, returning the previous value.
	// It panics on out-of-range index.
	Set(index int, value uint16) uint16
}

// CharSet marker interface for set-like collections (uniqueness implied).
// Satisfied by: CharHashSet, CharTreeSet, ImmutableCharHashSet.
type CharSet interface {
	CharCollection
}

// CharMutableSet adds insertion. Add returns true if the value was newly inserted.
// Satisfied by: CharHashSet, CharTreeSet.
type CharMutableSet interface {
	CharSet
	CharMutableCollection

	// Add inserts a value. Returns true if the value was not already present.
	Add(value uint16) bool
}

// CharBag read-only multiset interface with occurrence counts.
// Satisfied by: CharHashBag, CharTreeBag, ImmutableCharHashBag.
type CharBag interface {
	CharCollection

	// OccurrencesOf returns the number of times value occurs in the bag.
	OccurrencesOf(value uint16) int

	// SizeDistinct returns the number of *distinct* values (ignoring multiplicity).
	SizeDistinct() int
}

// CharMutableBag adds insertion.
// Satisfied by: CharHashBag, CharTreeBag.
type CharMutableBag interface {
	CharBag
	CharMutableCollection

	// Add adds one occurrence of value. Returns true always (a bag always accepts
	// the element); the bool is the CharAdder contract.
	Add(value uint16) bool
}

// CharStack read-only LIFO stack. Peek returns the top element or an error if empty.
// Satisfied by: CharArrayStack, ImmutableCharArrayStack.
type CharStack interface {
	CharCollection

	// Peek returns the top element without removing it. The bool is false if empty.
	Peek() (uint16, bool)
}

// CharMutableStack mutable LIFO stack with Push and Pop.
// Satisfied by: CharArrayStack.
type CharMutableStack interface {
	CharStack
	CharMutableCollection

	// Push pushes a value onto the top of the stack.
	Push(value uint16)

	// Pop removes and returns the top element. The bool is false if empty.
	Pop() (uint16, bool)
}

// ── Framework capability interfaces ────────────────────────────────────

// CharAdder is the single-element insertion capability shared by every
// mutable non-stack collection (CharArrayList, CharHashSet,
// CharHashBag, CharTreeSet, CharTreeBag, …). It is the sink a bulk
// loader targets: the bool reports collection-specific insertion info (lists/bags
// always true, sets whether newly inserted) and bulk loaders ignore it.
type CharAdder interface {
	Add(value uint16) bool
}
