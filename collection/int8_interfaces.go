// Code generated by internal/codegen; DO NOT EDIT.

package collection

import (
	"fmt"
	"iter"
)

// ── Composable sub-interfaces ─────────────────────────────────────────
//
// Following Go's io.Reader / io.Writer / io.ReadWriter pattern, the
// primitive-collection API is built from small, single-concern
// interfaces that can be composed as needed.

// Int8Sized exposes the element count of a collection.
type Int8Sized interface {
	// Len returns the number of elements. Use x.Len() == 0 to test for
	// emptiness.
	Len() int
}

// Int8Iterable provides element-by-element traversal.
type Int8Iterable interface {
	// All returns an iter.Seq that yields all elements.
	All() iter.Seq[int8]

	// ForEach calls the given function for each element.
	ForEach(f func(int8))
}

// Int8Searchable supports membership and predicate queries.
type Int8Searchable interface {
	// Contains returns true if the collection contains the given value.
	Contains(value int8) bool

	// AnySatisfy returns true if any element satisfies the predicate.
	AnySatisfy(predicate func(int8) bool) bool

	// AllSatisfy returns true if all elements satisfy the predicate.
	AllSatisfy(predicate func(int8) bool) bool

	// NoneSatisfy returns true if no element satisfies the predicate.
	NoneSatisfy(predicate func(int8) bool) bool
}

// Int8Convertible supports bulk conversion to a slice.
type Int8Convertible interface {
	// ToSlice returns all elements as a slice.
	ToSlice() []int8
}

// ── Composed collection interfaces ────────────────────────────────────

// Int8Collection is the full read-only interface for any collection of
// int8 values.  It composes the smaller sub-interfaces above, so a
// caller that only needs iteration can accept Int8Iterable while code
// that needs everything can accept Int8Collection.
//
// Satisfied by: Int8ArrayList, Int8HashSet, Int8HashBag,
// Int8ArrayStack, Int8TreeSet, and their immutable variants.
type Int8Collection interface {
	Int8Sized
	Int8Iterable
	Int8Searchable
	Int8Convertible
	fmt.Stringer
}

// Int8MutableCollection extends Int8Collection with mutation operations.
// Satisfied by: Int8ArrayList, Int8HashSet, Int8HashBag, Int8ArrayStack.
type Int8MutableCollection interface {
	Int8Collection

	// Clear removes all elements.
	Clear()
}

// ── Category interfaces ────────────────────────────────────────────────
//
// These distinguish *what kind of collection* is required without naming
// a concrete type. They mirror Java's IntList / IntSet / IntBag / IntStack
// hierarchy and the matching trait/comptime layers in Rust and Zig.
//
// Note: every mutable non-stack category exposes the unified Add(value) bool
// (the Int8Adder capability): lists and bags always return true (they always
// accept the element), sets return whether the value was newly inserted. Stacks
// use Push instead. The bool is collection-specific insertion info that bulk
// loaders (seq.Into) ignore.

// Int8List is the read-only interface for ordered lists with positional
// access. Satisfied by: Int8ArrayList, ImmutableInt8ArrayList.
type Int8List interface {
	Int8Collection

	// Get returns the element at the given index. It panics on out-of-range index.
	Get(index int) int8

	// IndexOf returns the index of the first occurrence of value, or -1 if absent.
	IndexOf(value int8) int
}

// Int8MutableList extends Int8List + Int8MutableCollection.
// Satisfied by: Int8ArrayList.
type Int8MutableList interface {
	Int8List
	Int8MutableCollection

	// Add appends a value to the end of the list. Returns true always (a list
	// always accepts the element); the bool is the Int8Adder contract.
	Add(value int8) bool

	// Set sets the value at the given index, returning the previous value.
	// It panics on out-of-range index.
	Set(index int, value int8) int8
}

// Int8Set marker interface for set-like collections (uniqueness implied).
// Satisfied by: Int8HashSet, Int8TreeSet, ImmutableInt8HashSet.
type Int8Set interface {
	Int8Collection
}

// Int8MutableSet adds insertion. Add returns true if the value was newly inserted.
// Satisfied by: Int8HashSet, Int8TreeSet.
type Int8MutableSet interface {
	Int8Set
	Int8MutableCollection

	// Add inserts a value. Returns true if the value was not already present.
	Add(value int8) bool
}

// Int8Bag read-only multiset interface with occurrence counts.
// Satisfied by: Int8HashBag, Int8TreeBag, ImmutableInt8HashBag.
type Int8Bag interface {
	Int8Collection

	// OccurrencesOf returns the number of times value occurs in the bag.
	OccurrencesOf(value int8) int

	// SizeDistinct returns the number of *distinct* values (ignoring multiplicity).
	SizeDistinct() int
}

// Int8MutableBag adds insertion.
// Satisfied by: Int8HashBag, Int8TreeBag.
type Int8MutableBag interface {
	Int8Bag
	Int8MutableCollection

	// Add adds one occurrence of value. Returns true always (a bag always accepts
	// the element); the bool is the Int8Adder contract.
	Add(value int8) bool
}

// Int8Stack read-only LIFO stack. Peek returns the top element or an error if empty.
// Satisfied by: Int8ArrayStack, ImmutableInt8ArrayStack.
type Int8Stack interface {
	Int8Collection

	// Peek returns the top element without removing it. The bool is false if empty.
	Peek() (int8, bool)
}

// Int8MutableStack mutable LIFO stack with Push and Pop.
// Satisfied by: Int8ArrayStack.
type Int8MutableStack interface {
	Int8Stack
	Int8MutableCollection

	// Push pushes a value onto the top of the stack.
	Push(value int8)

	// Pop removes and returns the top element. The bool is false if empty.
	Pop() (int8, bool)
}

// ── Framework capability interfaces ────────────────────────────────────

// Int8Adder is the single-element insertion capability shared by every
// mutable non-stack collection (Int8ArrayList, Int8HashSet,
// Int8HashBag, Int8TreeSet, Int8TreeBag, …). It is the sink a bulk
// loader targets: the bool reports collection-specific insertion info (lists/bags
// always true, sets whether newly inserted) and bulk loaders ignore it.
type Int8Adder interface {
	Add(value int8) bool
}
