// Code generated by internal/codegen; DO NOT EDIT.

package hashmap

import (
	"fmt"
	"iter"
	"math"
	"strings"
)

// Float64Int16BiMap is a bidirectional map with float64 keys and int16 values.
// Both key-to-value and value-to-key lookups are O(1).
type Float64Int16BiMap struct {
	forward *Float64Int16
	reverse *Int16Float64
}

// NewFloat64Int16BiMap creates a new empty Float64Int16BiMap with default capacity.
func NewFloat64Int16BiMap() *Float64Int16BiMap {
	return &Float64Int16BiMap{
		forward: NewFloat64Int16(),
		reverse: NewInt16Float64(),
	}
}

// NewFloat64Int16BiMapWithCapacity creates a new empty Float64Int16BiMap with the given initial capacity.
func NewFloat64Int16BiMapWithCapacity(capacity int) *Float64Int16BiMap {
	return &Float64Int16BiMap{
		forward: NewFloat64Int16WithCapacity(capacity),
		reverse: NewInt16Float64WithCapacity(capacity),
	}
}

// Put inserts or updates a key-value pair in both directions.
// If the key already existed, the old value mapping is removed from the reverse map.
// If the value already existed as a value for a different key, that old key mapping is removed.
// Returns the previous value and true if the key existed.
func (m *Float64Int16BiMap) Put(key float64, value int16) (int16, bool) {
	// If this value is already mapped to a different key, remove that old key->value pair
	if oldKey, ok := m.reverse.Get(value); ok {
		if !(math.Float64bits(oldKey) == math.Float64bits(key)) {
			m.forward.Remove(oldKey)
		}
	}

	// If this key already has a value, remove the old value->key reverse mapping
	oldVal, existed := m.forward.Get(key)
	if existed {
		m.reverse.Remove(oldVal)
	}

	m.forward.Put(key, value)
	m.reverse.Put(value, key)
	return oldVal, existed
}

// Get returns the value for the given key and true if found, or the zero value and false if not.
func (m *Float64Int16BiMap) Get(key float64) (int16, bool) {
	return m.forward.Get(key)
}

// GetKey returns the key for the given value and true if found, or the zero value and false if not.
func (m *Float64Int16BiMap) GetKey(value int16) (float64, bool) {
	return m.reverse.Get(value)
}

// Remove deletes the entry for the given key from both directions.
// Returns the previous value and true if the key existed.
func (m *Float64Int16BiMap) Remove(key float64) (int16, bool) {
	oldVal, existed := m.forward.Remove(key)
	if existed {
		m.reverse.Remove(oldVal)
	}
	return oldVal, existed
}

// RemoveValue deletes the entry for the given value from both directions.
// Returns the previous key and true if the value existed.
func (m *Float64Int16BiMap) RemoveValue(value int16) (float64, bool) {
	oldKey, existed := m.reverse.Remove(value)
	if existed {
		m.forward.Remove(oldKey)
	}
	return oldKey, existed
}

// ContainsKey returns true if the map contains the given key.
func (m *Float64Int16BiMap) ContainsKey(key float64) bool {
	return m.forward.ContainsKey(key)
}

// ContainsValue returns true if the map contains the given value.
func (m *Float64Int16BiMap) ContainsValue(value int16) bool {
	return m.reverse.ContainsKey(value)
}

// Len returns the number of elements. Use m.Len() == 0 to test for emptiness.
func (m *Float64Int16BiMap) Len() int {
	return m.forward.Len()
}

// Clear removes all entries from both directions.
func (m *Float64Int16BiMap) Clear() {
	m.forward.Clear()
	m.reverse.Clear()
}

// ForEach calls the given function for each key-value pair.
func (m *Float64Int16BiMap) ForEach(f func(float64, int16)) {
	m.forward.ForEach(f)
}

// Keys returns an iter.Seq that yields all keys.
func (m *Float64Int16BiMap) Keys() iter.Seq[float64] {
	return m.forward.Keys()
}

// Values returns an iter.Seq that yields all values.
func (m *Float64Int16BiMap) Values() iter.Seq[int16] {
	return m.forward.Values()
}

// Inverse returns a new Int16Float64BiMap with keys and values swapped.
func (m *Float64Int16BiMap) Inverse() *Int16Float64BiMap {
	result := NewInt16Float64BiMap()
	m.forward.ForEach(func(k float64, v int16) {
		result.Put(v, k)
	})
	return result
}

// String returns a string representation of the bi-map.
func (m *Float64Int16BiMap) String() string {
	if m.forward.Len() == 0 {
		return "{}"
	}
	var sb strings.Builder
	sb.WriteString("{")
	first := true
	m.forward.ForEach(func(k float64, v int16) {
		if !first {
			sb.WriteString(", ")
		}
		fmt.Fprintf(&sb, "%v: %v", k, v)
		first = false
	})
	sb.WriteString("}")
	return sb.String()
}

// Equals returns true if the other bi-map has the same key-value pairs.
func (m *Float64Int16BiMap) Equals(other *Float64Int16BiMap) bool {
	return m.forward.Equals(other.forward)
}
