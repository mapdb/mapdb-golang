// Code generated by internal/codegen; DO NOT EDIT.

package interval_test

import (
	"testing"

	"github.com/mapdb/mapdb-golang/internal/conformance"
	"github.com/mapdb/mapdb-golang/interval"
)

// TestConformanceAllMatchesToSliceInt8 pins law 1 (todo 14 §4) against
// an INDEPENDENT oracle. This family builds ToSlice() by ranging All(), so the
// All ≡ ToSlice form would hold no matter how All() broke; the expected
// elements are spelled out instead.
func TestConformanceAllMatchesToSliceInt8(t *testing.T) {
	c := interval.NewInt8(1, 8, 1)
	conformance.AllMatchesLiteral(t, c.All(), []int8{int8(1), int8(2), int8(3), int8(4), int8(5), int8(6), int8(7), int8(8)})
	// ToSlice() is held to the same oracle, so if it ever becomes an
	// independent implementation it cannot drift from All() unnoticed.
	conformance.SliceMatchesLiteral(t, c.ToSlice(), []int8{int8(1), int8(2), int8(3), int8(4), int8(5), int8(6), int8(7), int8(8)})
}

// TestConformanceSegmentsInt8 pins the Segments partition law (todo
// 14 §4): concat(Segments(n)) ≡ All() as a multiset and each segment is
// re-runnable, for n ∈ {1, 2, 7, len+1}.
func TestConformanceSegmentsInt8(t *testing.T) {
	c := interval.NewInt8(1, 8, 1)
	conformance.SegmentsCoverAll(t, c.All(), c.Segments)
}

// TestConformanceAllMatchesToSliceInt16 pins law 1 (todo 14 §4) against
// an INDEPENDENT oracle. This family builds ToSlice() by ranging All(), so the
// All ≡ ToSlice form would hold no matter how All() broke; the expected
// elements are spelled out instead.
func TestConformanceAllMatchesToSliceInt16(t *testing.T) {
	c := interval.NewInt16(1, 8, 1)
	conformance.AllMatchesLiteral(t, c.All(), []int16{int16(1), int16(2), int16(3), int16(4), int16(5), int16(6), int16(7), int16(8)})
	// ToSlice() is held to the same oracle, so if it ever becomes an
	// independent implementation it cannot drift from All() unnoticed.
	conformance.SliceMatchesLiteral(t, c.ToSlice(), []int16{int16(1), int16(2), int16(3), int16(4), int16(5), int16(6), int16(7), int16(8)})
}

// TestConformanceSegmentsInt16 pins the Segments partition law (todo
// 14 §4): concat(Segments(n)) ≡ All() as a multiset and each segment is
// re-runnable, for n ∈ {1, 2, 7, len+1}.
func TestConformanceSegmentsInt16(t *testing.T) {
	c := interval.NewInt16(1, 8, 1)
	conformance.SegmentsCoverAll(t, c.All(), c.Segments)
}

// TestConformanceAllMatchesToSliceInt32 pins law 1 (todo 14 §4) against
// an INDEPENDENT oracle. This family builds ToSlice() by ranging All(), so the
// All ≡ ToSlice form would hold no matter how All() broke; the expected
// elements are spelled out instead.
func TestConformanceAllMatchesToSliceInt32(t *testing.T) {
	c := interval.NewInt32(1, 8, 1)
	conformance.AllMatchesLiteral(t, c.All(), []int32{int32(1), int32(2), int32(3), int32(4), int32(5), int32(6), int32(7), int32(8)})
	// ToSlice() is held to the same oracle, so if it ever becomes an
	// independent implementation it cannot drift from All() unnoticed.
	conformance.SliceMatchesLiteral(t, c.ToSlice(), []int32{int32(1), int32(2), int32(3), int32(4), int32(5), int32(6), int32(7), int32(8)})
}

// TestConformanceSegmentsInt32 pins the Segments partition law (todo
// 14 §4): concat(Segments(n)) ≡ All() as a multiset and each segment is
// re-runnable, for n ∈ {1, 2, 7, len+1}.
func TestConformanceSegmentsInt32(t *testing.T) {
	c := interval.NewInt32(1, 8, 1)
	conformance.SegmentsCoverAll(t, c.All(), c.Segments)
}

// TestConformanceAllMatchesToSliceInt64 pins law 1 (todo 14 §4) against
// an INDEPENDENT oracle. This family builds ToSlice() by ranging All(), so the
// All ≡ ToSlice form would hold no matter how All() broke; the expected
// elements are spelled out instead.
func TestConformanceAllMatchesToSliceInt64(t *testing.T) {
	c := interval.NewInt64(1, 8, 1)
	conformance.AllMatchesLiteral(t, c.All(), []int64{int64(1), int64(2), int64(3), int64(4), int64(5), int64(6), int64(7), int64(8)})
	// ToSlice() is held to the same oracle, so if it ever becomes an
	// independent implementation it cannot drift from All() unnoticed.
	conformance.SliceMatchesLiteral(t, c.ToSlice(), []int64{int64(1), int64(2), int64(3), int64(4), int64(5), int64(6), int64(7), int64(8)})
}

// TestConformanceSegmentsInt64 pins the Segments partition law (todo
// 14 §4): concat(Segments(n)) ≡ All() as a multiset and each segment is
// re-runnable, for n ∈ {1, 2, 7, len+1}.
func TestConformanceSegmentsInt64(t *testing.T) {
	c := interval.NewInt64(1, 8, 1)
	conformance.SegmentsCoverAll(t, c.All(), c.Segments)
}
