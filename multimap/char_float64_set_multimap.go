// Code generated by internal/codegen; DO NOT EDIT.

package multimap

import (
	"fmt"
	"math"
	"strings"
)

// CharFloat64SetMultimap is a set multimap from uint16 keys to float64 values.
// Each key maps to a set of unique values (duplicates on Put are silently dropped).
type CharFloat64SetMultimap struct {
	data map[uint16][]float64
	size int
}

// NewCharFloat64SetMultimap creates a new empty CharFloat64SetMultimap.
func NewCharFloat64SetMultimap() *CharFloat64SetMultimap {
	return &CharFloat64SetMultimap{
		data: make(map[uint16][]float64),
		size: 0,
	}
}

// Put adds a value to the set for the given key. Idempotent: a duplicate
// value for the same key is silently dropped.
func (m *CharFloat64SetMultimap) Put(key uint16, value float64) {
	if m.data == nil {
		m.data = make(map[uint16][]float64)
	}
	for _, existing := range m.data[key] {
		if math.Float64bits(existing) == math.Float64bits(value) {
			return
		}
	}
	m.data[key] = append(m.data[key], value)
	m.size++
}

// Get returns a copy of the values for the given key. Returns nil if the key is absent.
func (m *CharFloat64SetMultimap) Get(key uint16) []float64 {
	return m.GetAll(key)
}

// GetAll returns a copy of the values for the given key.
func (m *CharFloat64SetMultimap) GetAll(key uint16) []float64 {
	vals := m.data[key]
	if vals == nil {
		return nil
	}
	result := make([]float64, len(vals))
	copy(result, vals)
	return result
}

// RemoveAll removes all values for the given key and returns them.
func (m *CharFloat64SetMultimap) RemoveAll(key uint16) []float64 {
	vals, ok := m.data[key]
	if !ok {
		return nil
	}
	delete(m.data, key)
	m.size -= len(vals)
	return vals
}

// ContainsKey returns true if the multimap contains the given key.
func (m *CharFloat64SetMultimap) ContainsKey(key uint16) bool {
	_, ok := m.data[key]
	return ok
}

// ContainsKeyValue returns true if the multimap contains the given key-value pair.
func (m *CharFloat64SetMultimap) ContainsKeyValue(key uint16, value float64) bool {
	vals, ok := m.data[key]
	if !ok {
		return false
	}
	for _, v := range vals {
		if math.Float64bits(v) == math.Float64bits(value) {
			return true
		}
	}
	return false
}

// KeysCount returns the number of distinct keys.
func (m *CharFloat64SetMultimap) KeysCount() int {
	return len(m.data)
}

// Size returns the total number of values across all keys.
func (m *CharFloat64SetMultimap) Size() int {
	return m.size
}

// Len returns the number of elements. It is an alias for Size, matching
// Go convention (sort.Interface, container/list, bytes.Buffer).
func (m *CharFloat64SetMultimap) Len() int { return m.Size() }

// IsEmpty returns true if the multimap contains no values.
func (m *CharFloat64SetMultimap) IsEmpty() bool {
	return m.size == 0
}

// Clear removes all entries from the multimap.
func (m *CharFloat64SetMultimap) Clear() {
	m.data = make(map[uint16][]float64)
	m.size = 0
}

// ForEach calls the given function for each key-value pair.
func (m *CharFloat64SetMultimap) ForEach(f func(uint16, float64)) {
	for key, vals := range m.data {
		for _, val := range vals {
			f(key, val)
		}
	}
}

// ForEachKeyValues calls the given function for each key with a copy of its values.
func (m *CharFloat64SetMultimap) ForEachKeyValues(f func(uint16, []float64)) {
	for key, vals := range m.data {
		copied := make([]float64, len(vals))
		copy(copied, vals)
		f(key, copied)
	}
}

// Keys returns a slice of all distinct keys.
func (m *CharFloat64SetMultimap) Keys() []uint16 {
	result := make([]uint16, 0, len(m.data))
	for key := range m.data {
		result = append(result, key)
	}
	return result
}

// Values returns a slice of all values across all keys.
func (m *CharFloat64SetMultimap) Values() []float64 {
	result := make([]float64, 0, m.size)
	for _, vals := range m.data {
		result = append(result, vals...)
	}
	return result
}

// Select returns a new multimap containing only key-value pairs that satisfy the predicate.
func (m *CharFloat64SetMultimap) Select(predicate func(uint16, float64) bool) *CharFloat64SetMultimap {
	result := NewCharFloat64SetMultimap()
	for key, vals := range m.data {
		for _, val := range vals {
			if predicate(key, val) {
				result.Put(key, val)
			}
		}
	}
	return result
}

// Reject returns a new multimap containing only key-value pairs that do not satisfy the predicate.
func (m *CharFloat64SetMultimap) Reject(predicate func(uint16, float64) bool) *CharFloat64SetMultimap {
	result := NewCharFloat64SetMultimap()
	for key, vals := range m.data {
		for _, val := range vals {
			if !predicate(key, val) {
				result.Put(key, val)
			}
		}
	}
	return result
}

// String returns a string representation of the multimap.
func (m *CharFloat64SetMultimap) String() string {
	if m.size == 0 {
		return "{}"
	}
	var sb strings.Builder
	sb.WriteString("{")
	first := true
	for key, vals := range m.data {
		if !first {
			sb.WriteString(", ")
		}
		fmt.Fprintf(&sb, "%v=[", key)
		for i, val := range vals {
			if i > 0 {
				sb.WriteString(", ")
			}
			fmt.Fprintf(&sb, "%v", val)
		}
		sb.WriteString("]")
		first = false
	}
	sb.WriteString("}")
	return sb.String()
}

// Equals returns true if the other multimap has the same key-value pairs in the same order per key.
func (m *CharFloat64SetMultimap) Equals(other *CharFloat64SetMultimap) bool {
	if m.size != other.size {
		return false
	}
	if len(m.data) != len(other.data) {
		return false
	}
	for key, vals := range m.data {
		otherVals, ok := other.data[key]
		if !ok || len(vals) != len(otherVals) {
			return false
		}
		for i, val := range vals {
			if !(math.Float64bits(val) == math.Float64bits(otherVals[i])) {
				return false
			}
		}
	}
	return true
}

// KeysToSlice returns all distinct keys as a slice.
func (m *CharFloat64SetMultimap) KeysToSlice() []uint16 {
	return m.Keys()
}

// ValuesToSlice returns all values as a slice.
func (m *CharFloat64SetMultimap) ValuesToSlice() []float64 {
	return m.Values()
}

// WithKeyValue adds a key-value pair and returns the multimap (fluent API).
func (m *CharFloat64SetMultimap) WithKeyValue(key uint16, value float64) *CharFloat64SetMultimap {
	m.Put(key, value)
	return m
}

// WithoutKey removes all values for the key and returns the multimap (fluent API).
func (m *CharFloat64SetMultimap) WithoutKey(key uint16) *CharFloat64SetMultimap {
	m.RemoveAll(key)
	return m
}
