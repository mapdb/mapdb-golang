// Code generated by internal/codegen; DO NOT EDIT.

package multimap

import "math"

// cmpKeyFloat32 is the three-way ordering for float32 multimap keys used
// by the FromSorted bulk-load validators. For float keys it is the IEEE-754 total order (matching the
// tree families), so NaN sorts to the top and ±0 stay distinct.
func cmpKeyFloat32(a, b float32) int {
	ai := int32(math.Float32bits(a))
	bi := int32(math.Float32bits(b))
	ai ^= int32(uint32(ai>>31) >> 1)
	bi ^= int32(uint32(bi>>31) >> 1)
	switch {
	case ai < bi:
		return -1
	case ai > bi:
		return 1
	default:
		return 0
	}
}
