// Code generated by internal/codegen; DO NOT EDIT.

package multimap

import (
	"fmt"
	"strings"
)

// Int64Int32Set is a set multimap from int64 keys to int32 values.
// Each key maps to a set of unique values (duplicates on Put are silently dropped).
type Int64Int32Set struct {
	data map[int64][]int32
	size int
}

// NewInt64Int32Set creates a new empty Int64Int32Set.
func NewInt64Int32Set() *Int64Int32Set {
	return &Int64Int32Set{
		data: make(map[int64][]int32),
		size: 0,
	}
}

// Put adds a value to the set for the given key. Idempotent: a duplicate
// value for the same key is silently dropped.
func (m *Int64Int32Set) Put(key int64, value int32) {
	if m.data == nil {
		m.data = make(map[int64][]int32)
	}
	for _, existing := range m.data[key] {
		if existing == value {
			return
		}
	}
	m.data[key] = append(m.data[key], value)
	m.size++
}

// Get returns a copy of the values for the given key. Returns nil if the key is absent.
func (m *Int64Int32Set) Get(key int64) []int32 {
	return m.GetAll(key)
}

// GetAll returns a copy of the values for the given key.
func (m *Int64Int32Set) GetAll(key int64) []int32 {
	vals := m.data[key]
	if vals == nil {
		return nil
	}
	result := make([]int32, len(vals))
	copy(result, vals)
	return result
}

// RemoveAll removes all values for the given key and returns them.
func (m *Int64Int32Set) RemoveAll(key int64) []int32 {
	vals, ok := m.data[key]
	if !ok {
		return nil
	}
	delete(m.data, key)
	m.size -= len(vals)
	return vals
}

// ContainsKey returns true if the multimap contains the given key.
func (m *Int64Int32Set) ContainsKey(key int64) bool {
	_, ok := m.data[key]
	return ok
}

// ContainsKeyValue returns true if the multimap contains the given key-value pair.
func (m *Int64Int32Set) ContainsKeyValue(key int64, value int32) bool {
	vals, ok := m.data[key]
	if !ok {
		return false
	}
	for _, v := range vals {
		if v == value {
			return true
		}
	}
	return false
}

// KeysCount returns the number of distinct keys.
func (m *Int64Int32Set) KeysCount() int {
	return len(m.data)
}

// Len returns the number of elements. Use m.Len() == 0 to test for emptiness.
func (m *Int64Int32Set) Len() int {
	return m.size
}

// Clear removes all entries from the multimap.
func (m *Int64Int32Set) Clear() {
	m.data = make(map[int64][]int32)
	m.size = 0
}

// ForEach calls the given function for each key-value pair.
func (m *Int64Int32Set) ForEach(f func(int64, int32)) {
	for key, vals := range m.data {
		for _, val := range vals {
			f(key, val)
		}
	}
}

// ForEachKeyValues calls the given function for each key with a copy of its values.
func (m *Int64Int32Set) ForEachKeyValues(f func(int64, []int32)) {
	for key, vals := range m.data {
		copied := make([]int32, len(vals))
		copy(copied, vals)
		f(key, copied)
	}
}

// Keys returns a slice of all distinct keys.
func (m *Int64Int32Set) Keys() []int64 {
	result := make([]int64, 0, len(m.data))
	for key := range m.data {
		result = append(result, key)
	}
	return result
}

// Values returns a slice of all values across all keys.
func (m *Int64Int32Set) Values() []int32 {
	result := make([]int32, 0, m.size)
	for _, vals := range m.data {
		result = append(result, vals...)
	}
	return result
}

// Select returns a new multimap containing only key-value pairs that satisfy the predicate.
func (m *Int64Int32Set) Select(predicate func(int64, int32) bool) *Int64Int32Set {
	result := NewInt64Int32Set()
	for key, vals := range m.data {
		for _, val := range vals {
			if predicate(key, val) {
				result.Put(key, val)
			}
		}
	}
	return result
}

// Reject returns a new multimap containing only key-value pairs that do not satisfy the predicate.
func (m *Int64Int32Set) Reject(predicate func(int64, int32) bool) *Int64Int32Set {
	result := NewInt64Int32Set()
	for key, vals := range m.data {
		for _, val := range vals {
			if !predicate(key, val) {
				result.Put(key, val)
			}
		}
	}
	return result
}

// String returns a string representation of the multimap.
func (m *Int64Int32Set) String() string {
	if m.size == 0 {
		return "{}"
	}
	var sb strings.Builder
	sb.WriteString("{")
	first := true
	for key, vals := range m.data {
		if !first {
			sb.WriteString(", ")
		}
		fmt.Fprintf(&sb, "%v=[", key)
		for i, val := range vals {
			if i > 0 {
				sb.WriteString(", ")
			}
			fmt.Fprintf(&sb, "%v", val)
		}
		sb.WriteString("]")
		first = false
	}
	sb.WriteString("}")
	return sb.String()
}

// Equals returns true if the other multimap has the same key-value pairs in the same order per key.
func (m *Int64Int32Set) Equals(other *Int64Int32Set) bool {
	if m.size != other.size {
		return false
	}
	if len(m.data) != len(other.data) {
		return false
	}
	for key, vals := range m.data {
		otherVals, ok := other.data[key]
		if !ok || len(vals) != len(otherVals) {
			return false
		}
		for i, val := range vals {
			if !(val == otherVals[i]) {
				return false
			}
		}
	}
	return true
}

// KeysToSlice returns all distinct keys as a slice.
func (m *Int64Int32Set) KeysToSlice() []int64 {
	return m.Keys()
}

// ValuesToSlice returns all values as a slice.
func (m *Int64Int32Set) ValuesToSlice() []int32 {
	return m.Values()
}

// PutReturning adds a key-value pair and returns the multimap (fluent API).
func (m *Int64Int32Set) PutReturning(key int64, value int32) *Int64Int32Set {
	m.Put(key, value)
	return m
}

// RemoveKeyReturning removes all values for the key and returns the multimap (fluent API).
func (m *Int64Int32Set) RemoveKeyReturning(key int64) *Int64Int32Set {
	m.RemoveAll(key)
	return m
}
