// Code generated by internal/codegen; DO NOT EDIT.

package treemap

import (
	"fmt"
	"iter"
	"strings"

	"github.com/mapdb/mapdb-golang/pump"
)

const (
	float32CharTreeNodeRed   = false
	float32CharTreeNodeBlack = true
)

type float32CharTreeNode struct {
	key    float32
	value  uint16
	left   *float32CharTreeNode
	right  *float32CharTreeNode
	parent *float32CharTreeNode
	color  bool
}

// Float32Char is a sorted map with float32 keys and uint16 values, backed by a red-black tree.
// Keys are maintained in ascending order.
type Float32Char struct {
	root *float32CharTreeNode
	size int
}

// NewFloat32Char creates a new empty sorted map.
func NewFloat32Char() *Float32Char {
	return &Float32Char{}
}

// Put inserts or updates a key-value pair. Returns the previous value and true if the key existed.
func (m *Float32Char) Put(key float32, value uint16) (uint16, bool) {
	if m.root == nil {
		m.root = &float32CharTreeNode{key: key, value: value, color: float32CharTreeNodeBlack}
		m.size++
		return 0, false
	}
	node := m.root
	for {
		if cmpFloat32(key, node.key) < 0 {
			if node.left == nil {
				node.left = &float32CharTreeNode{key: key, value: value, parent: node, color: float32CharTreeNodeRed}
				m.fixAfterInsert(node.left)
				m.size++
				return 0, false
			}
			node = node.left
		} else if cmpFloat32(key, node.key) > 0 {
			if node.right == nil {
				node.right = &float32CharTreeNode{key: key, value: value, parent: node, color: float32CharTreeNodeRed}
				m.fixAfterInsert(node.right)
				m.size++
				return 0, false
			}
			node = node.right
		} else {
			old := node.value
			node.value = value
			return old, true
		}
	}
}

// Get returns the value for the key, or the zero value and false if not found.
func (m *Float32Char) Get(key float32) (uint16, bool) {
	node := m.findNode(key)
	if node == nil {
		return 0, false
	}
	return node.value, true
}

// GetOrDefault returns the value for the key if present, or the default value otherwise.
func (m *Float32Char) GetOrDefault(key float32, defaultValue uint16) uint16 {
	if v, ok := m.Get(key); ok {
		return v
	}
	return defaultValue
}

// ContainsKey returns true if the map contains the given key.
func (m *Float32Char) ContainsKey(key float32) bool {
	return m.findNode(key) != nil
}

// Remove removes the entry for the given key. Returns the previous value and true if found.
func (m *Float32Char) Remove(key float32) (uint16, bool) {
	node := m.findNode(key)
	if node == nil {
		return 0, false
	}
	old := node.value
	m.deleteNode(node)
	m.size--
	return old, true
}

// Len returns the number of elements. Use m.Len() == 0 to test for emptiness.
func (m *Float32Char) Len() int {
	return m.size
}

// Clear removes all entries.
func (m *Float32Char) Clear() {
	m.root = nil
	m.size = 0
}

// Min returns the smallest key and its value, or zero values and false if empty.
func (m *Float32Char) Min() (float32, uint16, bool) {
	if m.root == nil {
		return 0, 0, false
	}
	node := m.minNode(m.root)
	return node.key, node.value, true
}

// Max returns the largest key and its value, or zero values and false if empty.
func (m *Float32Char) Max() (float32, uint16, bool) {
	if m.root == nil {
		return 0, 0, false
	}
	node := m.maxNode(m.root)
	return node.key, node.value, true
}

// Floor returns the largest key <= the given key, or zero values and false.
func (m *Float32Char) Floor(key float32) (float32, uint16, bool) {
	var result *float32CharTreeNode
	node := m.root
	for node != nil {
		if cmpFloat32(key, node.key) == 0 {
			return node.key, node.value, true
		}
		if cmpFloat32(key, node.key) > 0 {
			result = node
			node = node.right
		} else {
			node = node.left
		}
	}
	if result == nil {
		return 0, 0, false
	}
	return result.key, result.value, true
}

// Ceiling returns the smallest key >= the given key, or zero values and false.
func (m *Float32Char) Ceiling(key float32) (float32, uint16, bool) {
	var result *float32CharTreeNode
	node := m.root
	for node != nil {
		if cmpFloat32(key, node.key) == 0 {
			return node.key, node.value, true
		}
		if cmpFloat32(key, node.key) < 0 {
			result = node
			node = node.left
		} else {
			node = node.right
		}
	}
	if result == nil {
		return 0, 0, false
	}
	return result.key, result.value, true
}

// All returns an iter.Seq2 that yields all key-value pairs in ascending key order.
func (m *Float32Char) All() iter.Seq2[float32, uint16] {
	return func(yield func(float32, uint16) bool) {
		var inorder func(node *float32CharTreeNode) bool
		inorder = func(node *float32CharTreeNode) bool {
			if node == nil {
				return true
			}
			if !inorder(node.left) {
				return false
			}
			if !yield(node.key, node.value) {
				return false
			}
			return inorder(node.right)
		}
		inorder(m.root)
	}
}

// Keys returns an iter.Seq that yields all keys in ascending order.
func (m *Float32Char) Keys() iter.Seq[float32] {
	return func(yield func(float32) bool) {
		for k, _ := range m.All() {
			if !yield(k) {
				return
			}
		}
	}
}

// Values returns an iter.Seq that yields all values in key order.
func (m *Float32Char) Values() iter.Seq[uint16] {
	return func(yield func(uint16) bool) {
		for _, v := range m.All() {
			if !yield(v) {
				return
			}
		}
	}
}

// RangeKeys returns an iter.Seq2 that yields entries with keys in [fromKey, toKey).
func (m *Float32Char) RangeKeys(fromKey, toKey float32) iter.Seq2[float32, uint16] {
	return func(yield func(float32, uint16) bool) {
		for k, v := range m.All() {
			if cmpFloat32(k, fromKey) < 0 {
				continue
			}
			if cmpFloat32(k, toKey) >= 0 {
				return
			}
			if !yield(k, v) {
				return
			}
		}
	}
}

// Higher returns the smallest key strictly greater than `key` (and its value),
// or zero values and false. Unlike Ceiling, never returns `key` itself.
func (m *Float32Char) Higher(key float32) (float32, uint16, bool) {
	var result *float32CharTreeNode
	node := m.root
	for node != nil {
		if cmpFloat32(key, node.key) < 0 {
			result = node
			node = node.left
		} else {
			node = node.right
		}
	}
	if result == nil {
		return 0, 0, false
	}
	return result.key, result.value, true
}

// Lower returns the largest key strictly less than `key` (and its value),
// or zero values and false. Unlike Floor, never returns `key` itself.
func (m *Float32Char) Lower(key float32) (float32, uint16, bool) {
	var result *float32CharTreeNode
	node := m.root
	for node != nil {
		if cmpFloat32(key, node.key) > 0 {
			result = node
			node = node.right
		} else {
			node = node.left
		}
	}
	if result == nil {
		return 0, 0, false
	}
	return result.key, result.value, true
}

// HeadMap returns an iter.Seq2 over entries with keys strictly less than toKey.
// Matches Java NavigableMap.headMap(toKey) (exclusive by default).
func (m *Float32Char) HeadMap(toKey float32) iter.Seq2[float32, uint16] {
	return func(yield func(float32, uint16) bool) {
		for k, v := range m.All() {
			if cmpFloat32(k, toKey) >= 0 {
				return
			}
			if !yield(k, v) {
				return
			}
		}
	}
}

// TailMap returns an iter.Seq2 over entries with keys >= fromKey.
// Matches Java NavigableMap.tailMap(fromKey) (inclusive by default).
func (m *Float32Char) TailMap(fromKey float32) iter.Seq2[float32, uint16] {
	return func(yield func(float32, uint16) bool) {
		for k, v := range m.All() {
			if cmpFloat32(k, fromKey) < 0 {
				continue
			}
			if !yield(k, v) {
				return
			}
		}
	}
}

// SubMap returns an iter.Seq2 over entries with keys in [fromKey, toKey).
// Alias for RangeKeys; exists for Java-NavigableMap API parity.
func (m *Float32Char) SubMap(fromKey, toKey float32) iter.Seq2[float32, uint16] {
	return m.RangeKeys(fromKey, toKey)
}

// FirstEntry is an alias of Min — the smallest key and its value, or zero/false.
func (m *Float32Char) FirstEntry() (float32, uint16, bool) { return m.Min() }

// LastEntry is an alias of Max — the largest key and its value, or zero/false.
func (m *Float32Char) LastEntry() (float32, uint16, bool) { return m.Max() }

// PollFirstEntry removes and returns the smallest entry, or zero/false if empty.
func (m *Float32Char) PollFirstEntry() (float32, uint16, bool) {
	k, v, ok := m.Min()
	if !ok {
		return 0, 0, false
	}
	m.Remove(k)
	return k, v, true
}

// PollLastEntry removes and returns the largest entry, or zero/false if empty.
func (m *Float32Char) PollLastEntry() (float32, uint16, bool) {
	k, v, ok := m.Max()
	if !ok {
		return 0, 0, false
	}
	m.Remove(k)
	return k, v, true
}

// DescendingMap returns an iter.Seq2 over entries in descending key order.
func (m *Float32Char) DescendingMap() iter.Seq2[float32, uint16] {
	return func(yield func(float32, uint16) bool) {
		var reverse func(node *float32CharTreeNode) bool
		reverse = func(node *float32CharTreeNode) bool {
			if node == nil {
				return true
			}
			if !reverse(node.right) {
				return false
			}
			if !yield(node.key, node.value) {
				return false
			}
			return reverse(node.left)
		}
		reverse(m.root)
	}
}

// DescendingKeys returns an iter.Seq over keys in descending order.
func (m *Float32Char) DescendingKeys() iter.Seq[float32] {
	return func(yield func(float32) bool) {
		for k := range m.DescendingMap() {
			if !yield(k) {
				return
			}
		}
	}
}

// ForEach calls the function for each key-value pair in ascending order.
func (m *Float32Char) ForEach(f func(float32, uint16)) {
	for k, v := range m.All() {
		f(k, v)
	}
}

// Select returns a new map with entries satisfying the predicate.
func (m *Float32Char) Select(predicate func(float32, uint16) bool) *Float32Char {
	result := NewFloat32Char()
	for k, v := range m.All() {
		if predicate(k, v) {
			result.Put(k, v)
		}
	}
	return result
}

// Reject returns a new map with entries NOT satisfying the predicate.
func (m *Float32Char) Reject(predicate func(float32, uint16) bool) *Float32Char {
	result := NewFloat32Char()
	for k, v := range m.All() {
		if !predicate(k, v) {
			result.Put(k, v)
		}
	}
	return result
}

// Detect returns the first entry satisfying the predicate (in key order), or (zero, zero, false).
func (m *Float32Char) Detect(predicate func(float32, uint16) bool) (float32, uint16, bool) {
	for k, v := range m.All() {
		if predicate(k, v) {
			return k, v, true
		}
	}
	var zk float32
	var zv uint16
	return zk, zv, false
}

// AnySatisfy returns true if any entry satisfies the predicate.
func (m *Float32Char) AnySatisfy(predicate func(float32, uint16) bool) bool {
	for k, v := range m.All() {
		if predicate(k, v) {
			return true
		}
	}
	return false
}

// AllSatisfy returns true if all entries satisfy the predicate.
func (m *Float32Char) AllSatisfy(predicate func(float32, uint16) bool) bool {
	for k, v := range m.All() {
		if !predicate(k, v) {
			return false
		}
	}
	return true
}

// NoneSatisfy returns true if no entry satisfies the predicate.
func (m *Float32Char) NoneSatisfy(predicate func(float32, uint16) bool) bool {
	for k, v := range m.All() {
		if predicate(k, v) {
			return false
		}
	}
	return true
}

// Count returns the number of entries satisfying the predicate.
func (m *Float32Char) Count(predicate func(float32, uint16) bool) int {
	c := 0
	for k, v := range m.All() {
		if predicate(k, v) {
			c++
		}
	}
	return c
}

// String returns a string representation with entries in sorted key order.
func (m *Float32Char) String() string {
	if m.size == 0 {
		return "{}"
	}
	var sb strings.Builder
	sb.WriteString("{")
	first := true
	for k, v := range m.All() {
		if !first {
			sb.WriteString(", ")
		}
		fmt.Fprintf(&sb, "%v: %v", k, v)
		first = false
	}
	sb.WriteString("}")
	return sb.String()
}

// --- Red-black tree internals ---

func (m *Float32Char) findNode(key float32) *float32CharTreeNode {
	node := m.root
	for node != nil {
		if cmpFloat32(key, node.key) < 0 {
			node = node.left
		} else if cmpFloat32(key, node.key) > 0 {
			node = node.right
		} else {
			return node
		}
	}
	return nil
}

func (m *Float32Char) minNode(node *float32CharTreeNode) *float32CharTreeNode {
	for node.left != nil {
		node = node.left
	}
	return node
}

func (m *Float32Char) maxNode(node *float32CharTreeNode) *float32CharTreeNode {
	for node.right != nil {
		node = node.right
	}
	return node
}

func (m *Float32Char) rotateLeft(x *float32CharTreeNode) {
	y := x.right
	x.right = y.left
	if y.left != nil {
		y.left.parent = x
	}
	y.parent = x.parent
	if x.parent == nil {
		m.root = y
	} else if x == x.parent.left {
		x.parent.left = y
	} else {
		x.parent.right = y
	}
	y.left = x
	x.parent = y
}

func (m *Float32Char) rotateRight(x *float32CharTreeNode) {
	y := x.left
	x.left = y.right
	if y.right != nil {
		y.right.parent = x
	}
	y.parent = x.parent
	if x.parent == nil {
		m.root = y
	} else if x == x.parent.right {
		x.parent.right = y
	} else {
		x.parent.left = y
	}
	y.right = x
	x.parent = y
}

func (m *Float32Char) fixAfterInsert(z *float32CharTreeNode) {
	for z.parent != nil && z.parent.color == float32CharTreeNodeRed {
		if z.parent == z.parent.parent.left {
			y := z.parent.parent.right
			if y != nil && y.color == float32CharTreeNodeRed {
				z.parent.color = float32CharTreeNodeBlack
				y.color = float32CharTreeNodeBlack
				z.parent.parent.color = float32CharTreeNodeRed
				z = z.parent.parent
			} else {
				if z == z.parent.right {
					z = z.parent
					m.rotateLeft(z)
				}
				z.parent.color = float32CharTreeNodeBlack
				z.parent.parent.color = float32CharTreeNodeRed
				m.rotateRight(z.parent.parent)
			}
		} else {
			y := z.parent.parent.left
			if y != nil && y.color == float32CharTreeNodeRed {
				z.parent.color = float32CharTreeNodeBlack
				y.color = float32CharTreeNodeBlack
				z.parent.parent.color = float32CharTreeNodeRed
				z = z.parent.parent
			} else {
				if z == z.parent.left {
					z = z.parent
					m.rotateRight(z)
				}
				z.parent.color = float32CharTreeNodeBlack
				z.parent.parent.color = float32CharTreeNodeRed
				m.rotateLeft(z.parent.parent)
			}
		}
	}
	m.root.color = float32CharTreeNodeBlack
}

func (m *Float32Char) deleteNode(z *float32CharTreeNode) {
	if z.left != nil && z.right != nil {
		succ := m.minNode(z.right)
		z.key = succ.key
		z.value = succ.value
		z = succ
	}
	var child *float32CharTreeNode
	if z.left != nil {
		child = z.left
	} else {
		child = z.right
	}
	if child != nil {
		child.parent = z.parent
		if z.parent == nil {
			m.root = child
		} else if z == z.parent.left {
			z.parent.left = child
		} else {
			z.parent.right = child
		}
		if z.color == float32CharTreeNodeBlack {
			m.fixAfterDelete(child)
		}
	} else if z.parent == nil {
		m.root = nil
	} else {
		if z.color == float32CharTreeNodeBlack {
			m.fixAfterDelete(z)
		}
		if z.parent != nil {
			if z == z.parent.left {
				z.parent.left = nil
			} else {
				z.parent.right = nil
			}
		}
	}
}

// --- Data pump (bulk import) ---

// NewFloat32CharFromSorted builds a Float32Char from presorted, ascending keys
// in a single O(n) pass, skipping the per-insert rebalancing of Put. keys must
// be in ascending order according to the map's own comparator (the IEEE-754
// total order for float keys); keys[i] and values[i] form one entry, so the two
// slices must have equal length (a length mismatch is a programmer error and
// panics).
//
// On an out-of-order key it returns pump.ErrNotSorted. On a duplicate key
// it returns pump.ErrDuplicateKey unless policy is
// pump.IgnoreDuplicates, in which case the first value for a key is kept
// and the rest are skipped. A failed build returns a nil map and never a
// half-built one.
//
// The result is observably identical to the same entries inserted one-by-one
// with Put: same iteration order, same lookups. The tree is a valid red-black
// tree, so later Put/Remove preserve the invariant.
func NewFloat32CharFromSorted(keys []float32, values []uint16, policy pump.DuplicatePolicy) (*Float32Char, error) {
	if len(keys) != len(values) {
		panic("mapdb: NewFloat32CharFromSorted: len(keys) != len(values)")
	}
	dk, dv, err := dedupFloat32CharSorted(keys, values, policy)
	if err != nil {
		return nil, err
	}
	m := NewFloat32Char()
	m.root = m.buildfloat32Char(dk, dv, 0, len(dk)-1, 0, pump.RedBlackRedLevel(len(dk)), nil)
	m.size = len(dk)
	return m, nil
}

// dedupFloat32CharSorted validates ascending order and applies the duplicate
// policy, returning compacted key/value slices. Equal adjacent keys are the only
// duplicates possible in sorted input.
func dedupFloat32CharSorted(keys []float32, values []uint16, policy pump.DuplicatePolicy) ([]float32, []uint16, error) {
	if len(keys) == 0 {
		return keys, values, nil
	}
	outK := make([]float32, 0, len(keys))
	outV := make([]uint16, 0, len(keys))
	outK = append(outK, keys[0])
	outV = append(outV, values[0])
	for i := 1; i < len(keys); i++ {
		cmp := cmpFloat32(keys[i], keys[i-1])
		if cmp < 0 {
			return nil, nil, pump.ErrNotSorted
		}
		if cmp == 0 {
			if policy == pump.IgnoreDuplicates {
				continue
			}
			return nil, nil, pump.ErrDuplicateKey
		}
		outK = append(outK, keys[i])
		outV = append(outV, values[i])
	}
	return outK, outV, nil
}

// buildfloat32Char recursively builds a perfectly balanced subtree from the
// sorted slices over [lo, hi], colouring nodes on redLevel red and all others
// black (the classic JDK buildFromSorted). parent wires the back-pointers.
func (m *Float32Char) buildfloat32Char(keys []float32, values []uint16, lo, hi, level, redLevel int, parent *float32CharTreeNode) *float32CharTreeNode {
	if lo > hi {
		return nil
	}
	mid := (lo + hi) / 2
	node := &float32CharTreeNode{key: keys[mid], value: values[mid], parent: parent, color: float32CharTreeNodeBlack}
	if level == redLevel {
		node.color = float32CharTreeNodeRed
	}
	node.left = m.buildfloat32Char(keys, values, lo, mid-1, level+1, redLevel, node)
	node.right = m.buildfloat32Char(keys, values, mid+1, hi, level+1, redLevel, node)
	return node
}

// Float32CharSink is a streaming builder for a Float32Char: callers Put
// ascending entries one at a time, then Build the finished map. It is a thin
// wrapper over NewFloat32CharFromSorted (entries are buffered, then built in one
// pass), so the build logic and contract live in one place. After an error, or
// after Build, the sink is poisoned and further Put/Build calls panic.
type Float32CharSink struct {
	keys   []float32
	values []uint16
	policy pump.DuplicatePolicy
	done   bool
}

// NewFloat32CharSink creates a streaming sink with the given duplicate policy.
func NewFloat32CharSink(policy pump.DuplicatePolicy) *Float32CharSink {
	return &Float32CharSink{policy: policy}
}

// Put appends one entry. Entries must be supplied in ascending key order; order
// and duplicate violations are reported by Build, not here (the buffer-then-build
// shape detects them in one pass). Calling Put after Build panics.
func (s *Float32CharSink) Put(key float32, value uint16) {
	if s.done {
		panic("mapdb: Put on a finished Float32CharSink")
	}
	s.keys = append(s.keys, key)
	s.values = append(s.values, value)
}

// Build finishes the sink and returns the map. The sink is poisoned afterwards
// (a second Build panics). On an order/duplicate error the map is nil and the
// sink is still poisoned.
func (s *Float32CharSink) Build() (*Float32Char, error) {
	if s.done {
		panic("mapdb: Build on a finished Float32CharSink")
	}
	s.done = true
	return NewFloat32CharFromSorted(s.keys, s.values, s.policy)
}

func (m *Float32Char) fixAfterDelete(x *float32CharTreeNode) {
	for x != m.root && x.color == float32CharTreeNodeBlack {
		if x == x.parent.left {
			w := x.parent.right
			if w == nil {
				x = x.parent
				continue
			}
			if w.color == float32CharTreeNodeRed {
				w.color = float32CharTreeNodeBlack
				x.parent.color = float32CharTreeNodeRed
				m.rotateLeft(x.parent)
				w = x.parent.right
			}
			if w == nil {
				x = x.parent
				continue
			}
			leftBlack := w.left == nil || w.left.color == float32CharTreeNodeBlack
			rightBlack := w.right == nil || w.right.color == float32CharTreeNodeBlack
			if leftBlack && rightBlack {
				w.color = float32CharTreeNodeRed
				x = x.parent
			} else {
				if rightBlack {
					if w.left != nil {
						w.left.color = float32CharTreeNodeBlack
					}
					w.color = float32CharTreeNodeRed
					m.rotateRight(w)
					w = x.parent.right
				}
				w.color = x.parent.color
				x.parent.color = float32CharTreeNodeBlack
				if w.right != nil {
					w.right.color = float32CharTreeNodeBlack
				}
				m.rotateLeft(x.parent)
				x = m.root
			}
		} else {
			w := x.parent.left
			if w == nil {
				x = x.parent
				continue
			}
			if w.color == float32CharTreeNodeRed {
				w.color = float32CharTreeNodeBlack
				x.parent.color = float32CharTreeNodeRed
				m.rotateRight(x.parent)
				w = x.parent.left
			}
			if w == nil {
				x = x.parent
				continue
			}
			leftBlack := w.left == nil || w.left.color == float32CharTreeNodeBlack
			rightBlack := w.right == nil || w.right.color == float32CharTreeNodeBlack
			if leftBlack && rightBlack {
				w.color = float32CharTreeNodeRed
				x = x.parent
			} else {
				if leftBlack {
					if w.right != nil {
						w.right.color = float32CharTreeNodeBlack
					}
					w.color = float32CharTreeNodeRed
					m.rotateLeft(w)
					w = x.parent.left
				}
				w.color = x.parent.color
				x.parent.color = float32CharTreeNodeBlack
				if w.left != nil {
					w.left.color = float32CharTreeNodeBlack
				}
				m.rotateRight(x.parent)
				x = m.root
			}
		}
	}
	x.color = float32CharTreeNodeBlack
}
