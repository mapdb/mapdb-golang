// Code generated by internal/codegen; DO NOT EDIT.

package treemap

import (
	"fmt"
	"iter"
	"strings"
)

const (
	float64Int32TreeNodeRed   = false
	float64Int32TreeNodeBlack = true
)

type float64Int32TreeNode struct {
	key    float64
	value  int32
	left   *float64Int32TreeNode
	right  *float64Int32TreeNode
	parent *float64Int32TreeNode
	color  bool
}

// Float64Int32 is a sorted map with float64 keys and int32 values, backed by a red-black tree.
// Keys are maintained in ascending order.
type Float64Int32 struct {
	root *float64Int32TreeNode
	size int
}

// NewFloat64Int32 creates a new empty sorted map.
func NewFloat64Int32() *Float64Int32 {
	return &Float64Int32{}
}

// Put inserts or updates a key-value pair. Returns the previous value and true if the key existed.
func (m *Float64Int32) Put(key float64, value int32) (int32, bool) {
	if m.root == nil {
		m.root = &float64Int32TreeNode{key: key, value: value, color: float64Int32TreeNodeBlack}
		m.size++
		return 0, false
	}
	node := m.root
	for {
		if cmpFloat64(key, node.key) < 0 {
			if node.left == nil {
				node.left = &float64Int32TreeNode{key: key, value: value, parent: node, color: float64Int32TreeNodeRed}
				m.fixAfterInsert(node.left)
				m.size++
				return 0, false
			}
			node = node.left
		} else if cmpFloat64(key, node.key) > 0 {
			if node.right == nil {
				node.right = &float64Int32TreeNode{key: key, value: value, parent: node, color: float64Int32TreeNodeRed}
				m.fixAfterInsert(node.right)
				m.size++
				return 0, false
			}
			node = node.right
		} else {
			old := node.value
			node.value = value
			return old, true
		}
	}
}

// Get returns the value for the key, or the zero value and false if not found.
func (m *Float64Int32) Get(key float64) (int32, bool) {
	node := m.findNode(key)
	if node == nil {
		return 0, false
	}
	return node.value, true
}

// GetOrDefault returns the value for the key if present, or the default value otherwise.
func (m *Float64Int32) GetOrDefault(key float64, defaultValue int32) int32 {
	if v, ok := m.Get(key); ok {
		return v
	}
	return defaultValue
}

// ContainsKey returns true if the map contains the given key.
func (m *Float64Int32) ContainsKey(key float64) bool {
	return m.findNode(key) != nil
}

// Remove removes the entry for the given key. Returns the previous value and true if found.
func (m *Float64Int32) Remove(key float64) (int32, bool) {
	node := m.findNode(key)
	if node == nil {
		return 0, false
	}
	old := node.value
	m.deleteNode(node)
	m.size--
	return old, true
}

// Len returns the number of elements. Use m.Len() == 0 to test for emptiness.
func (m *Float64Int32) Len() int {
	return m.size
}

// Clear removes all entries.
func (m *Float64Int32) Clear() {
	m.root = nil
	m.size = 0
}

// Min returns the smallest key and its value, or zero values and false if empty.
func (m *Float64Int32) Min() (float64, int32, bool) {
	if m.root == nil {
		return 0, 0, false
	}
	node := m.minNode(m.root)
	return node.key, node.value, true
}

// Max returns the largest key and its value, or zero values and false if empty.
func (m *Float64Int32) Max() (float64, int32, bool) {
	if m.root == nil {
		return 0, 0, false
	}
	node := m.maxNode(m.root)
	return node.key, node.value, true
}

// Floor returns the largest key <= the given key, or zero values and false.
func (m *Float64Int32) Floor(key float64) (float64, int32, bool) {
	var result *float64Int32TreeNode
	node := m.root
	for node != nil {
		if cmpFloat64(key, node.key) == 0 {
			return node.key, node.value, true
		}
		if cmpFloat64(key, node.key) > 0 {
			result = node
			node = node.right
		} else {
			node = node.left
		}
	}
	if result == nil {
		return 0, 0, false
	}
	return result.key, result.value, true
}

// Ceiling returns the smallest key >= the given key, or zero values and false.
func (m *Float64Int32) Ceiling(key float64) (float64, int32, bool) {
	var result *float64Int32TreeNode
	node := m.root
	for node != nil {
		if cmpFloat64(key, node.key) == 0 {
			return node.key, node.value, true
		}
		if cmpFloat64(key, node.key) < 0 {
			result = node
			node = node.left
		} else {
			node = node.right
		}
	}
	if result == nil {
		return 0, 0, false
	}
	return result.key, result.value, true
}

// All returns an iter.Seq2 that yields all key-value pairs in ascending key order.
func (m *Float64Int32) All() iter.Seq2[float64, int32] {
	return func(yield func(float64, int32) bool) {
		var inorder func(node *float64Int32TreeNode) bool
		inorder = func(node *float64Int32TreeNode) bool {
			if node == nil {
				return true
			}
			if !inorder(node.left) {
				return false
			}
			if !yield(node.key, node.value) {
				return false
			}
			return inorder(node.right)
		}
		inorder(m.root)
	}
}

// Keys returns an iter.Seq that yields all keys in ascending order.
func (m *Float64Int32) Keys() iter.Seq[float64] {
	return func(yield func(float64) bool) {
		for k, _ := range m.All() {
			if !yield(k) {
				return
			}
		}
	}
}

// Values returns an iter.Seq that yields all values in key order.
func (m *Float64Int32) Values() iter.Seq[int32] {
	return func(yield func(int32) bool) {
		for _, v := range m.All() {
			if !yield(v) {
				return
			}
		}
	}
}

// RangeKeys returns an iter.Seq2 that yields entries with keys in [fromKey, toKey).
func (m *Float64Int32) RangeKeys(fromKey, toKey float64) iter.Seq2[float64, int32] {
	return func(yield func(float64, int32) bool) {
		for k, v := range m.All() {
			if cmpFloat64(k, fromKey) < 0 {
				continue
			}
			if cmpFloat64(k, toKey) >= 0 {
				return
			}
			if !yield(k, v) {
				return
			}
		}
	}
}

// Higher returns the smallest key strictly greater than `key` (and its value),
// or zero values and false. Unlike Ceiling, never returns `key` itself.
func (m *Float64Int32) Higher(key float64) (float64, int32, bool) {
	var result *float64Int32TreeNode
	node := m.root
	for node != nil {
		if cmpFloat64(key, node.key) < 0 {
			result = node
			node = node.left
		} else {
			node = node.right
		}
	}
	if result == nil {
		return 0, 0, false
	}
	return result.key, result.value, true
}

// Lower returns the largest key strictly less than `key` (and its value),
// or zero values and false. Unlike Floor, never returns `key` itself.
func (m *Float64Int32) Lower(key float64) (float64, int32, bool) {
	var result *float64Int32TreeNode
	node := m.root
	for node != nil {
		if cmpFloat64(key, node.key) > 0 {
			result = node
			node = node.right
		} else {
			node = node.left
		}
	}
	if result == nil {
		return 0, 0, false
	}
	return result.key, result.value, true
}

// HeadMap returns an iter.Seq2 over entries with keys strictly less than toKey.
// Matches Java NavigableMap.headMap(toKey) (exclusive by default).
func (m *Float64Int32) HeadMap(toKey float64) iter.Seq2[float64, int32] {
	return func(yield func(float64, int32) bool) {
		for k, v := range m.All() {
			if cmpFloat64(k, toKey) >= 0 {
				return
			}
			if !yield(k, v) {
				return
			}
		}
	}
}

// TailMap returns an iter.Seq2 over entries with keys >= fromKey.
// Matches Java NavigableMap.tailMap(fromKey) (inclusive by default).
func (m *Float64Int32) TailMap(fromKey float64) iter.Seq2[float64, int32] {
	return func(yield func(float64, int32) bool) {
		for k, v := range m.All() {
			if cmpFloat64(k, fromKey) < 0 {
				continue
			}
			if !yield(k, v) {
				return
			}
		}
	}
}

// SubMap returns an iter.Seq2 over entries with keys in [fromKey, toKey).
// Alias for RangeKeys; exists for Java-NavigableMap API parity.
func (m *Float64Int32) SubMap(fromKey, toKey float64) iter.Seq2[float64, int32] {
	return m.RangeKeys(fromKey, toKey)
}

// FirstEntry is an alias of Min — the smallest key and its value, or zero/false.
func (m *Float64Int32) FirstEntry() (float64, int32, bool) { return m.Min() }

// LastEntry is an alias of Max — the largest key and its value, or zero/false.
func (m *Float64Int32) LastEntry() (float64, int32, bool) { return m.Max() }

// PollFirstEntry removes and returns the smallest entry, or zero/false if empty.
func (m *Float64Int32) PollFirstEntry() (float64, int32, bool) {
	k, v, ok := m.Min()
	if !ok {
		return 0, 0, false
	}
	m.Remove(k)
	return k, v, true
}

// PollLastEntry removes and returns the largest entry, or zero/false if empty.
func (m *Float64Int32) PollLastEntry() (float64, int32, bool) {
	k, v, ok := m.Max()
	if !ok {
		return 0, 0, false
	}
	m.Remove(k)
	return k, v, true
}

// DescendingMap returns an iter.Seq2 over entries in descending key order.
func (m *Float64Int32) DescendingMap() iter.Seq2[float64, int32] {
	return func(yield func(float64, int32) bool) {
		var reverse func(node *float64Int32TreeNode) bool
		reverse = func(node *float64Int32TreeNode) bool {
			if node == nil {
				return true
			}
			if !reverse(node.right) {
				return false
			}
			if !yield(node.key, node.value) {
				return false
			}
			return reverse(node.left)
		}
		reverse(m.root)
	}
}

// DescendingKeys returns an iter.Seq over keys in descending order.
func (m *Float64Int32) DescendingKeys() iter.Seq[float64] {
	return func(yield func(float64) bool) {
		for k := range m.DescendingMap() {
			if !yield(k) {
				return
			}
		}
	}
}

// ForEach calls the function for each key-value pair in ascending order.
func (m *Float64Int32) ForEach(f func(float64, int32)) {
	for k, v := range m.All() {
		f(k, v)
	}
}

// Select returns a new map with entries satisfying the predicate.
func (m *Float64Int32) Select(predicate func(float64, int32) bool) *Float64Int32 {
	result := NewFloat64Int32()
	for k, v := range m.All() {
		if predicate(k, v) {
			result.Put(k, v)
		}
	}
	return result
}

// Reject returns a new map with entries NOT satisfying the predicate.
func (m *Float64Int32) Reject(predicate func(float64, int32) bool) *Float64Int32 {
	result := NewFloat64Int32()
	for k, v := range m.All() {
		if !predicate(k, v) {
			result.Put(k, v)
		}
	}
	return result
}

// Detect returns the first entry satisfying the predicate (in key order), or (zero, zero, false).
func (m *Float64Int32) Detect(predicate func(float64, int32) bool) (float64, int32, bool) {
	for k, v := range m.All() {
		if predicate(k, v) {
			return k, v, true
		}
	}
	var zk float64
	var zv int32
	return zk, zv, false
}

// AnySatisfy returns true if any entry satisfies the predicate.
func (m *Float64Int32) AnySatisfy(predicate func(float64, int32) bool) bool {
	for k, v := range m.All() {
		if predicate(k, v) {
			return true
		}
	}
	return false
}

// AllSatisfy returns true if all entries satisfy the predicate.
func (m *Float64Int32) AllSatisfy(predicate func(float64, int32) bool) bool {
	for k, v := range m.All() {
		if !predicate(k, v) {
			return false
		}
	}
	return true
}

// NoneSatisfy returns true if no entry satisfies the predicate.
func (m *Float64Int32) NoneSatisfy(predicate func(float64, int32) bool) bool {
	for k, v := range m.All() {
		if predicate(k, v) {
			return false
		}
	}
	return true
}

// Count returns the number of entries satisfying the predicate.
func (m *Float64Int32) Count(predicate func(float64, int32) bool) int {
	c := 0
	for k, v := range m.All() {
		if predicate(k, v) {
			c++
		}
	}
	return c
}

// String returns a string representation with entries in sorted key order.
func (m *Float64Int32) String() string {
	if m.size == 0 {
		return "{}"
	}
	var sb strings.Builder
	sb.WriteString("{")
	first := true
	for k, v := range m.All() {
		if !first {
			sb.WriteString(", ")
		}
		fmt.Fprintf(&sb, "%v: %v", k, v)
		first = false
	}
	sb.WriteString("}")
	return sb.String()
}

// --- Red-black tree internals ---

func (m *Float64Int32) findNode(key float64) *float64Int32TreeNode {
	node := m.root
	for node != nil {
		if cmpFloat64(key, node.key) < 0 {
			node = node.left
		} else if cmpFloat64(key, node.key) > 0 {
			node = node.right
		} else {
			return node
		}
	}
	return nil
}

func (m *Float64Int32) minNode(node *float64Int32TreeNode) *float64Int32TreeNode {
	for node.left != nil {
		node = node.left
	}
	return node
}

func (m *Float64Int32) maxNode(node *float64Int32TreeNode) *float64Int32TreeNode {
	for node.right != nil {
		node = node.right
	}
	return node
}

func (m *Float64Int32) rotateLeft(x *float64Int32TreeNode) {
	y := x.right
	x.right = y.left
	if y.left != nil {
		y.left.parent = x
	}
	y.parent = x.parent
	if x.parent == nil {
		m.root = y
	} else if x == x.parent.left {
		x.parent.left = y
	} else {
		x.parent.right = y
	}
	y.left = x
	x.parent = y
}

func (m *Float64Int32) rotateRight(x *float64Int32TreeNode) {
	y := x.left
	x.left = y.right
	if y.right != nil {
		y.right.parent = x
	}
	y.parent = x.parent
	if x.parent == nil {
		m.root = y
	} else if x == x.parent.right {
		x.parent.right = y
	} else {
		x.parent.left = y
	}
	y.right = x
	x.parent = y
}

func (m *Float64Int32) fixAfterInsert(z *float64Int32TreeNode) {
	for z.parent != nil && z.parent.color == float64Int32TreeNodeRed {
		if z.parent == z.parent.parent.left {
			y := z.parent.parent.right
			if y != nil && y.color == float64Int32TreeNodeRed {
				z.parent.color = float64Int32TreeNodeBlack
				y.color = float64Int32TreeNodeBlack
				z.parent.parent.color = float64Int32TreeNodeRed
				z = z.parent.parent
			} else {
				if z == z.parent.right {
					z = z.parent
					m.rotateLeft(z)
				}
				z.parent.color = float64Int32TreeNodeBlack
				z.parent.parent.color = float64Int32TreeNodeRed
				m.rotateRight(z.parent.parent)
			}
		} else {
			y := z.parent.parent.left
			if y != nil && y.color == float64Int32TreeNodeRed {
				z.parent.color = float64Int32TreeNodeBlack
				y.color = float64Int32TreeNodeBlack
				z.parent.parent.color = float64Int32TreeNodeRed
				z = z.parent.parent
			} else {
				if z == z.parent.left {
					z = z.parent
					m.rotateRight(z)
				}
				z.parent.color = float64Int32TreeNodeBlack
				z.parent.parent.color = float64Int32TreeNodeRed
				m.rotateLeft(z.parent.parent)
			}
		}
	}
	m.root.color = float64Int32TreeNodeBlack
}

func (m *Float64Int32) deleteNode(z *float64Int32TreeNode) {
	if z.left != nil && z.right != nil {
		succ := m.minNode(z.right)
		z.key = succ.key
		z.value = succ.value
		z = succ
	}
	var child *float64Int32TreeNode
	if z.left != nil {
		child = z.left
	} else {
		child = z.right
	}
	if child != nil {
		child.parent = z.parent
		if z.parent == nil {
			m.root = child
		} else if z == z.parent.left {
			z.parent.left = child
		} else {
			z.parent.right = child
		}
		if z.color == float64Int32TreeNodeBlack {
			m.fixAfterDelete(child)
		}
	} else if z.parent == nil {
		m.root = nil
	} else {
		if z.color == float64Int32TreeNodeBlack {
			m.fixAfterDelete(z)
		}
		if z.parent != nil {
			if z == z.parent.left {
				z.parent.left = nil
			} else {
				z.parent.right = nil
			}
		}
	}
}

func (m *Float64Int32) fixAfterDelete(x *float64Int32TreeNode) {
	for x != m.root && x.color == float64Int32TreeNodeBlack {
		if x == x.parent.left {
			w := x.parent.right
			if w == nil {
				x = x.parent
				continue
			}
			if w.color == float64Int32TreeNodeRed {
				w.color = float64Int32TreeNodeBlack
				x.parent.color = float64Int32TreeNodeRed
				m.rotateLeft(x.parent)
				w = x.parent.right
			}
			if w == nil {
				x = x.parent
				continue
			}
			leftBlack := w.left == nil || w.left.color == float64Int32TreeNodeBlack
			rightBlack := w.right == nil || w.right.color == float64Int32TreeNodeBlack
			if leftBlack && rightBlack {
				w.color = float64Int32TreeNodeRed
				x = x.parent
			} else {
				if rightBlack {
					if w.left != nil {
						w.left.color = float64Int32TreeNodeBlack
					}
					w.color = float64Int32TreeNodeRed
					m.rotateRight(w)
					w = x.parent.right
				}
				w.color = x.parent.color
				x.parent.color = float64Int32TreeNodeBlack
				if w.right != nil {
					w.right.color = float64Int32TreeNodeBlack
				}
				m.rotateLeft(x.parent)
				x = m.root
			}
		} else {
			w := x.parent.left
			if w == nil {
				x = x.parent
				continue
			}
			if w.color == float64Int32TreeNodeRed {
				w.color = float64Int32TreeNodeBlack
				x.parent.color = float64Int32TreeNodeRed
				m.rotateRight(x.parent)
				w = x.parent.left
			}
			if w == nil {
				x = x.parent
				continue
			}
			leftBlack := w.left == nil || w.left.color == float64Int32TreeNodeBlack
			rightBlack := w.right == nil || w.right.color == float64Int32TreeNodeBlack
			if leftBlack && rightBlack {
				w.color = float64Int32TreeNodeRed
				x = x.parent
			} else {
				if leftBlack {
					if w.right != nil {
						w.right.color = float64Int32TreeNodeBlack
					}
					w.color = float64Int32TreeNodeRed
					m.rotateLeft(w)
					w = x.parent.left
				}
				w.color = x.parent.color
				x.parent.color = float64Int32TreeNodeBlack
				if w.left != nil {
					w.left.color = float64Int32TreeNodeBlack
				}
				m.rotateRight(x.parent)
				x = m.root
			}
		}
	}
	x.color = float64Int32TreeNodeBlack
}
